version: '3.8'

services:
  gimmie:
    image: yourbr0ther/gimmie:latest
    ports:
      - "5010:5010"
    volumes:
      - ./data:/app/data
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - LOGIN_PASSWORD=${LOGIN_PASSWORD}
    restart: unless-stopped